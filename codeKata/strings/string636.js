function string(str) {
    var newstring = "";
    for (var i = str.length - 1; i >= 0; i--) {
        newstring += str[i];
    }
    console.log(newstring = newstring.replace(newstring[0], newstring[0].toUpperCase()));
}
string("jennifer");